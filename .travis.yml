language: cpp

matrix:
  include:
    - compiler: gcc
      env: MYCC=gcc-4.9 MYCXX=g++-4.9
      addons:
        apt:
          sources:
            - ubuntu-toolchain-r-test
          packages:
            - g++-4.9
    - compiler: gcc
      env: MYCC=gcc-5 MYCXX=g++-5
      addons:
        apt:
          sources:
            - ubuntu-toolchain-r-test
          packages:
            - g++-5
    - compiler: clang
      env: MYCC=clang-3.6 MYCXX=clang++-3.6
      addons:
        apt:
          sources:
            - ubuntu-toolchain-r-test
            - llvm-toolchain-precise-3.6
          packages:
            - clang-3.6
    - compiler: clang
      env: MYCC=clang-3.7 MYCXX=clang++-3.7
      addons:
        apt:
          sources:
            - ubuntu-toolchain-r-test
            - llvm-toolchain-precise-3.7
          packages:
            - clang-3.7
    
script: 
  - export CC=$MYCC
  - export CXX=$MYCXX
  - $CXX --version
  - cmake -DCMAKE_BUILD_TYPE=Debug && make && ./DSim
